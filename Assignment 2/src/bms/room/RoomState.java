package bms.room;

/**
 * Enum to represent the state of the room.
 */
public enum RoomState {
    /** open state. */
    OPEN,
    /** evacuate state. */
    EVACUATE,
    /** maintenance state. */
    MAINTENANCE,
    /** error state. */
    ERROR
}
